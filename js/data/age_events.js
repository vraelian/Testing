// js/data/age_events.js
/**
 * @fileoverview
 * Defines all static narrative "age" events triggered by game progression.
 * * [DEPRECATED] 
 * These static events have been replaced by the procedural 3-Era Age System 
 * handled dynamically in TimeService.js. This array is intentionally left empty 
 * to prevent legacy triggers while preserving imports in database.js.
 */
import { PERK_IDS } from './constants.js';

export const AGE_EVENTS = [];